import { styled, Box } from "@mui/material";

export const ModalWrapper = styled(Box)({
    boxShadow: "5px 5px 12px -6px gray",
    border: "solid 1px #e3e3e3",
    borderRadius: "10px",
    position: "absolute",
    background: "#e9e9e9",
});